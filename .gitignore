# Hugo build output — δεν χρειάζεται στο git, το Cloudflare Pages το χτίζει
public/
resources/
.hugo_build.lock

# OS
.DS_Store
Thumbs.db

# Playwright logs — στιγμιότυπα και traces από τοπικές δοκιμές, όχι πηγαίος κώδικας
.playwright-mcp/

# Cache του Claude Code — χρονοσφραγίδες και απόλυτες διαδρομές του μηχανήματος,
# τίποτα που να αφορά το site. Το repo είναι δημόσιο.
.claude/tsc-cache/

# Editor
.vscode/
*.swp

# Wrangler local dev cache (KV, state)
.wrangler/
